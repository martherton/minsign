Dashboard
<br>
<%= @user.fname %>
<%= @user.lname %>
<% @user.roles.each do |role| %>
<p><%= role.name %></p>
<% end %>
<%= link_to "Back to App", new_user_find_path(current_user.id) %>
<h4>Recent searches</h4>
<% @finds.each do |find| %>
	<%= link_to "Repeat Search",  user_wlinks_path(current_user.id, queryid: find.linkcat_id, query: find.searchterm)%>
	<%= find.searchterm %>
	<%= find.created_at %>
<% end %>	