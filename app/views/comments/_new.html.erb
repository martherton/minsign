<% @comment = Comment.new %>
<Div class="moreinfos" >

<%= simple_form_for @comment do |f| %>
	

	<%= f.input :contentcom, label: false, placeholder: "Mail us with comments or suggestions for new modules", id: "commentinput" %>
	
	<div class="hidlinkcat" >
	
		<%= f.input :user_id, input_html: { value: current_user.id} %>
		

	</div>

	<%= f.button :submit, 'Submit Feedback' %>

<% end %>
</Div>